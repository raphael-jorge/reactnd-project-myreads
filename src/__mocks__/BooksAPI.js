export const get = bookId =>
  Promise.resolve({});

export const getAll = () =>
  Promise.resolve([]);

export const update = (book, shelf) =>
  Promise.resolve({});

export const search = query =>
  Promise.resolve([]);
